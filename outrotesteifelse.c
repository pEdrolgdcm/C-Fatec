#include <stdio.h>
int main()
{
    int saldo = 2000; // atribuição
    if (saldo == 0)
    {
        printf("Voce esta quebrado...\n");
    }
    else
    {
        printf("Voce tem dinheiro na conta!\n");
    }
    return (0);
}